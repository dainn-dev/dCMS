# Copy to `.env.local` (gitignored). Never commit real values.
#
# Linear MCP (`mcp-remote`): set `AUTH_HEADER` to what that server expects
# (often `Bearer lin_api_...` — see https://linear.app/developers/graphql ).
# Direct Linear GraphQL (`api.linear.app`) uses the raw key without `Bearer`.

AUTH_HEADER=
